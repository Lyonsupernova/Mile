# Install Python (if not already installed)
brew install python

# install az cli cmd
brew install azure-cli

# install docker
brew install docker

# Verify Python version
python --version

# Verify pip version
pip --version

# Install Django
pip install django